{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && service apache2 start && tail -f /var/log/apache2/error.log",
    "restartPolicyType": "ON_FAILURE"
  }
}
